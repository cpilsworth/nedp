export default async function decorate(block) {
  // Row 1: image, Row 2: overlay (h1 + CTA), Row 3: text block
  // The CSS handles all positioning via nth-child selectors
  // No structural changes needed — the authored HTML matches the layout
}
