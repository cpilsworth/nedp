export default async function decorate(block) {
  // The footer-links block has two rows:
  //   Row 1: 4 cells — each with an h6 heading + ul list of links
  //   Row 2: 3 cells — logo, copyright text, social icons
  // No structural changes needed; CSS handles the layout.
}
